const assert=require('assert');const fs=require('fs');const os=require('os');const path=require('path');const {Updater}=require('../updater');const root=fs.mkdtempSync(path.join(os.tmpdir(),'upd-'));const u=new Updater({install_dir:root,user_data_dir:root});u._fetchJson('http://127.0.0.1/manifest.json').then(()=>{throw Error('accepted http')},e=>{assert.match(e.message,/HTTPS is required/);console.log('updater rejects HTTP runtime')});
