

## ⛔ 用户已上传 LaTeX 模板套件（最高优先级）
用户上传了以下模板文件，已复制到 `paper/`：
{{ bundle_lines }}
{{ cls_hint }}
**写作时必须使用这套用户提供的模板：**
- `.tex` 主文件（如 main.tex）→ 在其结构基础上填入章节内容（保留所有 preamble、宏包、命令）
- `.cls/.sty` 文件 → 不要修改，编译时会自动加载
- `.bib` 文件 → 直接 `\bibliography{文件名}` 引用
- 不要重写或替换为默认竞赛模板
