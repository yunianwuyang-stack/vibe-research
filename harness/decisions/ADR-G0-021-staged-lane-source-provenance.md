# ADR-G0-021: ?? staged lane source provenance

- **???** ??????????? gate?phase?release ???????
- **???** 2026-07-18
- **???** `G0-F-008`?`harness/scripts/g0_runner.py`?`tests/test_harness_g0.py`

## ??

??? lane ????????? evidence destination ?? reparse?hardlink?direct-parent ? directory-identity ???`run_lanes()` ? child receipt ???????? `staged_junit`??? `_commit_staged_evidence_file()` ? source ?? `is_file()` ? `copyfile()`???????? JUnit ????? staging leaf ? NTFS hardlink???? reparse point???????? destination ???????????????? SHA-256 ???? receipt validator?

????????????????????????????????? baseline copy ???? NTFS hardlink ???????? lane `PASS`?receipt validation `PASS`??? evidence ????? XML ?????? destination `st_nlink == 1`??????? `harness/evidence/G0/g0-p1-staged-source-integrity-20260718T124236Z/red-characterization.json`?

## ??

1. ? `_StagingDirectoryGuard` ??? `TemporaryDirectory` ? raw path?resolved path ??? identity ???????? evidence guard????? staged source ???? staging/evidence identity?
2. ?? `_assert_safe_staged_source()`?staged source ??? staging root ??????raw/resolved ????? bound staging root ??`lstat()` ???????? reparse point?? `st_nlink == 1`??? source ??? `False`????????????????????? fail-closed?
3. ??? JUnit ?????? staged JUnit?? commit ???????? staged source?stdout/stderr ???? provenance ???
4. `_commit_staged_evidence_file()` ?????? staging guard??????? helper ?? source provenance?
5. ???? NTFS hardlink ??? `run_lanes()` ????? JUnit ?? staging ????link count ? 2??????? evidence ?????? `lane_staged_source_hardlink_not_allowed`?

## ?????

- ????????? staged-source alias ???????? destination?junction?dangling reparse?descendant junction ? workspace-external staging ???
- Windows/???????? `lstat`?`resolve`?`parse`?`copyfile` ?????????????????????????? TOCTOU ???????????????? gate ? release ???????????????
- ??? `????.md`?bootstrap contract ? `phase-contract.lock`???? G0 ? `BLOCKED` ?????? F003/F004/F006/F007 ??? blocker?
- ??????????????external validation?sealed evaluator ? release qualification?
